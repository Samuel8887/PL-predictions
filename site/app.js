const labels={"premier-league":"Premier League",championship:"Championship","league-one":"League One"};
let data,colours={},league="premier-league";
const esc=s=>String(s).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));
const pct=n=>`${Math.round(n*100)}%`;
const colour=name=>colours[name]||'#52657b';
function card(f){const p=[f.home_win,f.draw,f.away_win], max=Math.max(...p), winner=max===p[0]?`${f.home_team} win`:max===p[1]?'Draw':`${f.away_team} win`;return `<article class="fixture"><div class="fixture-head">${esc(new Date(f.date+'T12:00').toLocaleDateString(undefined,{weekday:'short',month:'short',day:'numeric',year:'numeric'}))}${f.kickoff?' · '+esc(f.kickoff):''}</div><div class="teams"><span class="accent" style="--team:${colour(f.home_team)}">${esc(f.home_team)}</span><span>vs</span><span class="away"><span class="accent" style="--team:${colour(f.away_team)}">${esc(f.away_team)}</span></span></div><div class="prob-labels"><span>${pct(p[0])} home win</span><span>${pct(p[1])} draw</span><span>${pct(p[2])} away win</span></div><div class="bar" aria-label="Home win ${pct(p[0])}, draw ${pct(p[1])}, away win ${pct(p[2])}"><i class="home" style="width:${p[0]*100}%"></i><i class="draw" style="width:${p[1]*100}%"></i><i class="awaybar" style="width:${p[2]*100}%"></i></div><div class="summary">Most likely result: <strong>${esc(winner)}</strong> · Expected goals: ${f.expected_home_goals.toFixed(1)} – ${f.expected_away_goals.toFixed(1)}</div></article>`}
function render(){const fixtures=data.leagues[league]||[];document.querySelector('#status').textContent=`${labels[league]} · ${fixtures.length} reliable upcoming estimate${fixtures.length===1?'':'s'}`;document.querySelector('#fixtures').innerHTML=fixtures.length?fixtures.map(card).join(''):`<p class="empty">No reliable upcoming fixtures are available in the source data for this league. No fixtures have been invented.</p>`;document.querySelectorAll('button').forEach(b=>b.classList.toggle('active',b.dataset.league===league))}
document.querySelectorAll('button').forEach(b=>b.onclick=()=>{league=b.dataset.league;render()});Promise.all([fetch('data/predictions.json').then(r=>r.json()),fetch('data/team_colours.json').then(r=>r.json())]).then(([x,c])=>{data=x;colours=c;document.querySelector('#updated').textContent=`Last updated: ${new Date(x.generated_at).toLocaleString()}.`;render()}).catch(()=>document.querySelector('#status').textContent='Prediction data could not be loaded.');
